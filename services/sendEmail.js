const getUserById = require("../queries/getUserById")
const send = require("./aws-ses")

module.exports = async (userId, emailAddress, emailTemplate) => {
~
    // if (!emailAddress) {
    //     const user = await getUserById(userId)
    // }
    
    // let email = emailAddress || user.email

    // switch (template) {
    //     case value:
            
    //         break;
    
    //     default:
    //         break;
    // }

    // send(email, )
}